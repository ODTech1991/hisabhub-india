# Contributing to HisabHub

1. Create a feature branch.
2. Keep calculation logic separate from UI.
3. Add tests for new calculations.
4. Do not hard-code changing statutory rates throughout UI components.
5. Do not create fake government API integrations.
6. Run `npm test` and `npm run build` before submitting changes.
